/**
 * CMS module registrations. Importing this file registers every module
 * with the shared UI registry. The admin route imports this for
 * side-effects on bootstrap.
 */
import "./news";
import "./honor";
import "./achievements";
